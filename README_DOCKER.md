# Docker run

```bash
cp .env.example .env
# edit .env with your Binance / Telegram keys

docker compose up --build
```

- Bot: http://localhost:5000 for dashboard
- The bot and dashboard run as separate services.
